# --- Configuration File (QUAN TRỌNG!) ---
# Không bao giờ đưa file chứa thông tin nhạy cảm như mật khẩu lên Git.
config.ini

# --- Generated Data Files ---
# Các file JSON được tạo ra bởi script không phải là code nguồn.
# Chúng sẽ thay đổi mỗi khi script chạy.
*.json

# --- Python Virtual Environment ---
# Thư mục môi trường ảo có thể rất lớn và không cần thiết phải đưa lên Git.
# Nó có thể được tạo lại từ file requirements.txt.
/venv/
/env/
/.venv/
/ENV/
/env.bak/
/venv.bak/
/__pypackages__/

# --- Python Cache & Compiled Files ---
# Các file này được Python tự động tạo ra để tăng tốc độ.
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# --- IDE and Editor Configuration ---
# Cấu hình riêng của mỗi người cho trình soạn thảo code.
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- Log Files ---
# Các file log được tạo ra trong quá trình chạy hoặc debug.
*.log
logs/

# --- Selenium / WebDriver Logs ---
geckodriver.log
chromedriver.log

# --- Distribution / Packaging ---
# Các file và thư mục được tạo ra khi bạn đóng gói ứng dụng.
/build/
/dist/
/downloads/
/eggs/
*.egg-info/
/sdist/
/wheel/
*.egg
*.spec

# --- Operating System Files ---
.DS_Store
Thumbs.db



# ===================================================================
# 1. QUY TẮC CHUNG CHO CÁC DỰ ÁN NODE.JS
# ===================================================================

# Bỏ qua tất cả các thư mục node_modules trong toàn bộ project.
# Đây là quy tắc quan trọng nhất.
**/node_modules

# Các file log sinh ra trong quá trình phát triển hoặc lỗi.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Các file chứa biến môi trường.
# TUYỆT ĐỐI KHÔNG commit file chứa thông tin nhạy cảm.
# Thay vào đó, hãy tạo file .env.example để người khác biết cần tạo biến gì.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Các báo cáo kiểm thử (test coverage).
coverage
.nyc_output

# ===================================================================
# 2. CÁC FILE & THƯ MỤC BUILD CỤ THỂ
# ===================================================================

# Thư mục build của Extension (Vite)
/extension/dist

# Thư mục cache của Vite
/extension/.vite

# Thư mục build của Web App (Next.js)
/webapp/.next

# Thư mục xuất tĩnh của Web App (Next.js)
/webapp/out

# File định nghĩa kiểu tự động sinh ra bởi Next.js
/webapp/next-env.d.ts

# ===================================================================
# 3. CÁC FILE CỦA HỆ ĐIỀU HÀNH VÀ IDE
# ===================================================================

# Các file hệ thống của macOS
.DS_Store
.AppleDouble
.LSOverride

# Các file hệ thống của Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
*.stackdump

# Thư mục cấu hình của các IDE/Editor
# (Cho phép team chia sẻ các file cài đặt chung nếu cần)
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json